syntax = "v1"

type LoginRequest {
	Username string `json:"userName"`
	Password string `json:"password"`
}

@server (
	prefix: /api/user
)
service user {
	@handler login
	post /login (LoginRequest) returns (string)
}

