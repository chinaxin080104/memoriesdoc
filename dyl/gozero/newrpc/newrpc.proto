syntax = "proto3";

package newrpc;
option go_package="./newrpc";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Newrpc {
  rpc Ping(Request) returns(Response);
}
